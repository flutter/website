name: constraints
description: example for ui/layout/constraints
version: 1.0.0

environment:
  sdk: '>=2.12.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_lints: ^1.0.3

flutter:
  uses-material-design: true
