name: sqlite
description: Example of using sqflite plugin to access SQLite database.

environment:
  sdk: ^3.6.0-0

dependencies:
  flutter:
    sdk: flutter
  sqflite: ^2.3.2
  path: ^1.9.0

dev_dependencies:
  example_utils:
    path: ../../../example_utils
  flutter_test:
    sdk: flutter


flutter:
  uses-material-design: true
