name: form
description: Use Form widget to perform form validation.

resolution: workspace
environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
