name: network_image
description: Example for network_image cookbook recipe

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.0.3

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
