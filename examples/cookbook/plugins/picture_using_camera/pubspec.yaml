name: picture_using_camera
description: A new Flutter project.

environment:
  sdk: ^3.0.3

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.0
  path_provider: ^2.0.1
  path: ^1.8.0

dev_dependencies:
  flutter_lints: ^2.0.1

flutter:
  uses-material-design: true
