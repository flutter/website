name: mixed_lists
description: Example for mixed_lists cookbook recipe

resolution: workspace
environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
