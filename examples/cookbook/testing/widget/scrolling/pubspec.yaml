name: scrolling
description: Scrollable widget testing.

resolution: workspace
environment:
  sdk: ^3.6.0

dependencies:
  flutter:
    sdk: flutter
  path_provider: ^2.1.2
  path: ^1.9.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
